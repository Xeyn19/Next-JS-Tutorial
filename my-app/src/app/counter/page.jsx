import Counter from "./Counter";

export const metadata = {
    title: {
        absolute:"Counter",
    } 
}

export default function CounterPage () {
    return (
        <div className="">
            <Counter />
        </div>
    )
}